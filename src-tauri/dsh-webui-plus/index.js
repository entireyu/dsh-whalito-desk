// DSH WebUI+ 插件 —— node 半体（占位）。
// 全部用户可见行为在浏览器半体 client.js 中（经 dsh.client 装载缝）。
// 本插件 v1 为纯客户端实现：数据均来自客户端已有接口
// （connection.api.sessions.models / ConversationSnapshot / sessions / inputActions），
// 设置持久化在浏览器 localStorage，无需 host 侧服务。
export const inject = [];
export function apply() {}
